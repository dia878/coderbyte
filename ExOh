def ExOh(str)

  str2 = str.tr('x', 'o')
  strlen = str.length
  
  return !(!(strlen.modulo(2)))
         
end
   
# keep this function call here 
# to see how to enter arguments in Ruby scroll down   
ExOh(STDIN.gets)  
